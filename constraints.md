# Lifeline Church Website — Constraints & Guidelines

## Target Users & Maintainers

- **Website Content Managers:** Non-technical church volunteers. They will update the site entirely through the GitHub web browser interface by editing plain text files in the `content/` folder or uploading images to the `all_photos/` folder. They do not have local development environments, terminal access, or coding knowledge.
- **Future Developers:** Developers working on the codebase must preserve this simple, browser-editable architecture. Do not introduce complex frameworks that require technical users to build the site locally.

## Technology Constraints

- **The Website (Frontend):** Purely static HTML, CSS, and Vanilla JavaScript.
  - No React, Vue, Angular, Next.js, Vite, or bundlers. Keep it simple so anyone can edit the raw code if needed.
  - No Tailwind CSS or CSS preprocessors. Use plain CSS with CSS variables.
  - The site must be able to run locally by simply double-clicking `frontend/index.html` — no local server required to view the UI.
- **The Automation (Backend/Build):** We do use Node.js, but strictly for automation via GitHub Actions.
  - `repoSetup.js` is a Node.js utility script that runs automatically in the cloud via GitHub Actions whenever photos are uploaded. It scans folders and rebuilds `gallery.json`. Volunteers never run this manually.

## Content Management

- **All editable content lives in the `content/` folder as plain text files.** Non-technical users update the website by editing these simple `.txt` files — no coding or JSON syntax needed.
- **Photo gallery categories and file lists live in `frontend/gallery.json`.** This file also contains `land_imgs` and `abt_us_imgs` arrays. Auto-regenerated by a GitHub Action when you push changes.
- **The YouTube video auto-updates every Sunday at 3 PM EDT.** A GitHub Actions workflow (`.github/workflows/update-video.yml`) fetches the latest video and updates `content/video.txt` automatically. Requires a `YOUTUBE_API_KEY` GitHub Secret.

## Content Files (`content/` folder)

| File | What It Controls |
|---|---|
| `church_info.txt` | Church name, tagline, service times, contact info, welcome section |
| `about_us.txt` | About Us heading and paragraph |
| `events.txt` | Section heading + event list (blank lines separate events) |
| `ministries.txt` | Ministries heading, text, and links |
| `video.txt` | YouTube video ID (auto-updated by GitHub Actions) |
| `give.txt` | Give section heading, description, and options |
| `social_links.txt` | Facebook, Instagram, YouTube URLs |
| `map.txt` | Google Maps embed URL |
| `colors.txt` | Site color theme |
| `settings.txt` | Hero display, slideshow timing, scroll snap, photos header |

### Text File Format

All content files use a simple `key: value` format:
```
name: Lifeline Church
tagline: Reaching the unreached
```

For lists (events, give options), items are separated by **blank lines**:
```
heading: Upcoming Events

title: Sunday Service
description: Join us every Sunday at 5 PM.

title: Bible Study
description: Wednesday evenings at 7 PM.
```

## File Structure Rules

- **`all_photos/photos/`** — Contains subfolders for photo gallery categories. Drop images here; the GitHub Action handles the rest.
- **`all_photos/land_imgs/`** — Hero/landing background slideshow images.
- **`all_photos/abt_us_imgs/`** — About Us section images.
- **`frontend/`** — Contains the actual website code (`index.html`, `styles.css`, `index.js`), `gallery.json`, and `icons/`.
- **`content/`** — Editable text files for all website content.

## Adding New Photos

1. Upload the photo directly via the GitHub website into the appropriate folder (e.g., `all_photos/photos/outreach/`, `all_photos/land_imgs/`, or `all_photos/abt_us_imgs/`).
2. Commit the changes.
3. The GitHub Action will automatically run `repoSetup.js`, update `frontend/gallery.json`, and deploy the live site.

## Design Constraints

- Maintain a warm, clean, modern aesthetic with the existing design system.
- Use Google Fonts (Poppins for headings, Lato for body) — loaded via CDN.
- Use Font Awesome 5 icons via CDN.
- All photos must preserve their original aspect ratio (`object-fit: cover` for thumbnails, `object-fit: contain` for lightbox).
- The site must be fully responsive (desktop, tablet, mobile).

## Deployment

- Hosted on **GitHub Pages** using GitHub Actions (`.github/workflows/auto-update-gallery.yml`).
- The GitHub Action automatically runs `repoSetup.js` and deploys the generated site. Ensure the repository's Pages source is set to "GitHub Actions".
